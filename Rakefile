# Add your own tasks in files placed in lib/tasks with the .rake extension.
# Tasks are made available through the Rake file system and can be called with:
#
#   bin/rake taskname

require_relative "config/application"

Rails.application.load_tasks
