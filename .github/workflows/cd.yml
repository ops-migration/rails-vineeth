name: CD - Deploy to dev

on:
  push:
    branches:
      - dev-i

permissions:
  issues: write
  contents: read
  id-token: write
  
jobs:
  deploy-qa:
    uses: ops-migration/central-shared-workflow/.github/workflows/cd-workflow.yml@main
    with:
      cf-stack-name: "dev-stack"
